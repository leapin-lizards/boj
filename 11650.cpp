#include <iostream>
#include <stdio.h>
#include <bits/stdc++.h>
using namespace std;
int n, arr[2][100000] = {0};
int main()
{
    scanf("%d",&n);
    for (int i = 0; i < n; i++)
    {
        scanf("%d",&arr[0][i]);
        scanf("%d",&arr[1][i]);
    }
}