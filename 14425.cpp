#include <iostream>
#include <algorithm>
#include <map>
#include <string>
using namespace std;
string str[10000],strSearchFor[10000];

int main()
{
    int n,m;
    cin>>n>>m;
    map<string,int> m;
    for(int i=0;i<n;i++)
    {
        cin>>str[i];
        m.insert(make_pair())
    }
    for(int i=0;i<m;i++)
    {
        cin>>strSearchFor[i];
    }



}